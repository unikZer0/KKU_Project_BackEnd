upstream php-fpm {
    server app:9000;
}

server {
    listen 80;
    server_name localhost;

    root /var/www/html/public;
    index index.php index.html index.htm;
    client_max_body_size 50M;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|webp|ttf|woff|woff2)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass php-fpm;  # use the upstream name
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    location ~ /\. {
        deny all;
    }

    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
    gzip_min_length 256;
}



# server {
#     listen 80;
#     server_name yourdomain.com;  # Change to your domain or localhost

#     root /var/www/html/public;
#     index index.php index.html;

#     # Log files
#     access_log /var/log/nginx/access.log;
#     error_log /var/log/nginx/error.log;

#     # Serve static assets directly
#     location ~* \.(jpg|jpeg|gif|png|ico|css|js|svg|woff|woff2|ttf|eot|webp)$ {
#         expires 1y;
#         access_log off;
#         add_header Cache-Control "public";
#     }

#     # Gzip compression
#     gzip on;
#     gzip_types text/css text/javascript application/javascript application/json application/xml text/plain text/html;
#     gzip_min_length 256;

#     # Main Laravel routing
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }

#     # PHP-FPM handling
#     location ~ \.php$ {
#         include fastcgi_params;
#         fastcgi_pass laravel_app:9000; # Ensure your PHP-FPM container is named 'laravel_app'
#         fastcgi_index index.php;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_read_timeout 300;
#     }

#     # Deny access to hidden files and directories
#     location ~ /\.(?!well-known).* {
#         deny all;
#     }

#     # Optional: Prevent access to sensitive directories
#     location ~* /(vendor|storage)/ {
#         deny all;
#     }
# }

